# 首页与全书目录页建设记录

日期：2026-06-01

## 本轮目标

建设线上讲义总入口：首页 `/` 与完整目录页 `/outline/`，并将命题1样板页作为首个可访问内容节点接入导航。

## 已完成

- 新增首页封面 Hero 初版：深蓝背景、金色几何线条、椭圆轨道、焦点、扫面积扇形和几何网格。后续已由首页专属 AI 主视觉替换。
- 新增课程导语、三条推荐学习路径、当前样板页展示、目录概览、命题网络静态入口、资源与版权说明、项目状态。
- 新增完整目录页，支持章节折叠、搜索、类型筛选、状态筛选和状态文字徽标。
- 扩展 `scripts/build_outline_seed.py`，将第一、第二、第三定律纳入目录候选抽取。
- 重建 `content/metadata/principia_outline.seed.json`：共 337 个待校核候选项。
- 新增页面侧结构化目录转换层：按卷首与第一卷十四章整理为 321 个课程目录条目。
- 命题1样板页链接已接入 `/nodes/book1-chapter02/prop001-theorem001/`。
- 未建设条目均保持禁用占位，不生成死链接。
- 初版曾新增可编辑 SVG 背景，后续首页封面精修时已移除，避免与命题1样板页视觉故事重复。

## 验证

- `npm.cmd install`：通过。
- `python scripts/build_outline_seed.py`：通过，生成 337 个候选项。
- `python scripts/check_content_status.py`：通过。
- `npm.cmd run build`：通过，生成 `/`、`/outline/`、`/nodes/book1-chapter02/prop001-theorem001/` 三个静态页面。
- 本地 HTTP 检查：首页和目录页均返回 `200`。
- 桌面截图与首页窄屏截图：`exports/screenshots/homepage_outline/`。

## 待校核

- 自动提取目录仍需逐章人工核对，特别是无编号推论、附注和概要末尾补充参考部分。
- 第二章命题2至命题10尚未拆分为正式内容节点。
- 讲义公开授权边界与学校署名规范仍待确认。
