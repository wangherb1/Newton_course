# Web

本目录是 Newton Principia Modern Reading Course 的 Astro 网站工程。

技术栈：Astro + React + TypeScript + MDX。

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run build
```

课程结构化内容保存在项目根目录 `content/`，原始资料保存在 `sources/`，不得与网页源码混放。
